// import React from 'react';
// import ReactDOM from 'react-dom';
// import App from './App';

describe("calc", () => {
  it("should add numbers", () => {
    expect(2 + 2).toBe(4);
  });
  it("should add numbers", () => {
    expect(2 + 2).toBe(4);
  });
  it("should add numbers", () => {
    expect(2 + 2).toBe(4);
  });
  it("should add numbers", () => {
    expect(2 + 2).toBe(4);
  });
});
